source :rubygems

gem 'sinatra', '>= 1.0'
gem 'sinatra-content-for', :require => 'sinatra/content_for'
gem 'haml'

### json or json_pure
gem 'json'
# gem 'json_pure', :require => 'json'

gem 'therubyracer'   ## Comment out if Windows
gem 'coffee-script'
gem 'sprockets'
gem 'pikl'

group :development do
  gem 'sinatra-reloader', :require => 'sinatra/reloader'
end